services:
  toposcale:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: toposcale-rila-pirin
    container_name: toposcale-rila-pirin
    volumes:
      - .:/app
      - ~/.cdsapirc:/root/.cdsapirc:ro
      - ./docker/state/bash_history:/root/.bash_history_persist
    tty: true
    stdin_open: true
    command: sleep infinity