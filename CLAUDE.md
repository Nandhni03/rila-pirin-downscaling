
## Environment
This project requires the venv at `../venv` (relative to this repo) to be
active for any Python commands to work. If a command fails with
ModuleNotFoundError for a package that should be installed, check that
the venv is activated in the current shell before debugging further.
